1)авторизация Ldap
2)запрос из таблиц по user_id его роли--> возврат роли id проверка прав этой роли и выдвча прав
3)экспорт и импорт таблицы xls/строк-->запрос на поиск по проектам\сотрудникам\поддразделениям-->изьятие из таблицы данных строки(подразделение\проекты\сотрудники\детализация времени)(сотрудник\подразделение\его проекты и детализация)(проект\подразделение\сотрудники и детализация)-->вывод в таблицу
4)запрет на редакцию строки и всех данных связаных с ней
5)режим просмотра данных таблиц
6)отчет списком xls
7)график по таблице

3) +-+-+-returnInformation(time, needsInfotmation)

работа с таблицами:
0) ++getRoleName(userId)
1) ++getDepartmentNames(time)
1) ++getEmployeeNames(time)
2) ++getProjectNamesForDepartment(departmentName, time)
3) ++getEmployeeNamesForDepartment(departmentName, time)
4) ++getEployeeNamesAndPercentsForProject(projectName, departmentName, time)
5) ++getEmployeeInfo(employeeName, time)
6) ++getDepartmentHead(userId)

7) changeDepartmentName(departmentId, time, newDepartmentName)
9) changeProjectName(projectId, time, newProjectName)
0) changeEployeeTime(employeeId, projectId, time, newPercents)
2) changeEmployeeInfo(employeeId, time, newDepartmentId)
3) changeDepartmentHead(userId, departmentId)

))exportFiles(time)
4) xlsDepartmentNames(time)
5) xlsProjectNames(time)
6) xlsEmployeeNames(time)
7) xlsEmployeeInfo(time)

8) newDepartment(time, departmentName)
9) newProject(time, projectName, departmentId)
0) newEmployee(time, employeeName, departmentId)
1) newPercent(time, projectId, employeeId, newPercent)
2) newHeadDepartment(userId, departmentId)

8) deleteDepartment(time, departmentId)
9) deleteProject(time, projectId)
0) deleteEmployee(time, employeeId)
1) deletePercent(time, projectId, employeeId)

в каждой функции прописать ошибки и возврат через ключи и фич. полное оформление возврата.
pg_connect работает только надо содать php.ini и раскомитить нужное
подключение 


настроить удобное логирование раскидать инфо и ошибки в разные файлы желательно